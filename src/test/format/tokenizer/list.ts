import { expect } from "chai";
import list from "../../../format/tokenizer/list";
import init from "../../../parser/combinator/util/init";

describe("list test", () => {
  //   it("", () => expect(init(list)("@list\ntest".chars())).to.deep.equal({}));
});
